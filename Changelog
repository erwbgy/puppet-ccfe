0.1.2:

* Install perl-Curses dependency.

0.1.1:

* When a non-default group is set on a menu then ensure that the permissions
of files and directories are such that only the specified user and group are
allowed access.  This prevents access to menus by users who are not members of
the specified group.

0.1.0:

* Initial Puppet Forge release
